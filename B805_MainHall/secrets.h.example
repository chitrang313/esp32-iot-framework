#pragma once

// ─── B805 MainHall — secrets template ───────────────────────────────────────
// Copy this file to secrets.h and fill in real values.
// secrets.h is in .gitignore and must NEVER be committed.
//
// IMPORTANT: If any of these values were previously committed to git,
// rotate/revoke them before use.  A value in git history is already public.

#define SECRET_WIFI_SSID     ""    // Wi-Fi network name
#define SECRET_WIFI_PASSWORD ""    // Wi-Fi password

// Firebase project credentials — generate via Firebase Console → Project Settings
#define SECRET_FB_API_KEY    ""    // Web API Key
#define SECRET_FB_DB_URL     ""    // Realtime Database URL (no trailing slash)
#define SECRET_FB_EMAIL      ""    // Service/device account email
#define SECRET_FB_PASSWORD   ""    // Service/device account password
